---
phase: "02-gsd-db-rewrite"
plan: "01"
type: implementation
wave: 1
depends_on: ["01-storage-backend-abstraction"]
files_modified:
  - packages/pi-coding-agent/src/core/gsd-db.ts
  - packages/pi-coding-agent/src/core/storage-markdown.ts
autonomous: true
must_haves:
  truths:
    - MarkdownStorage implements full CRUD for milestones, slices, tasks
    - .md files are human-readable and git-diffable
    - Schema migration from SQLite → Markdown is supported
  artifacts:
    - storage-markdown.ts (full implementation)
    - gsd-db.ts (delegates to StorageBackend)
  key_links: []
---

<objective>
Rewrite gsd-db.ts to use StorageBackend and implement full MarkdownStorage for milestone, slice, task, unit, decision, and requirement CRUD operations.
</objective>

<tasks>
  <task>Implement MarkdownStorage for milestones (state.json → milestone.md)</task>
  <task>Implement MarkdownStorage for slices (slices.json → S##.md)</task>
  <task>Implement MarkdownStorage for tasks (tasks/T##.md)</task>
  <task>Implement MarkdownStorage for decisions (DECISIONS.md)</task>
  <task>Implement MarkdownStorage for requirements (REQUIREMENTS.md)</task>
  <task>Implement MarkdownStorage for units (unit claims as .md files)</task>
  <task>Add migration tool: SQLite → Markdown export</task>
  <task>Update gsd-db.ts to use createStorageBackend()</task>
</tasks>

<context>
File format decisions:
- Milestones: `.gsd/milestones/M###-*/state.json` → `.gsd/milestones/M###-*/STATE.md`
- Slices: `.gsd/slices.json` → `.gsd/slices/S##.md` (one per slice)
- Tasks: `.gsd/tasks/T##.md` (already MD format)
- Decisions: `.gsd/DECISIONS.md` (already MD format)
- Requirements: `.gsd/REQUIREMENTS.md` (already MD format)

JSON representation for internal use:
- Each .md file has YAML frontmatter + body
- Frontmatter contains structured data (status, depends_on, etc.)
- Body contains human-readable content

The migration tool should:
1. Read all data from SQLite
2. Write equivalent .md files
3. Verify round-trip consistency
4. Optionally delete .db files
</context>

<verification>
- [ ] All CRUD operations work with MarkdownStorage
- [ ] .md files are valid YAML frontmatter + markdown
- [ ] Migration from SQLite produces identical data
- [ ] Round-trip: read → write → read produces same data
- [ ] Existing gsd-db tests pass with both backends
</verification>

<success_criteria>
- MarkdownStorage handles all entity types
- Migration tool works end-to-end
- gsd-db.ts uses StorageBackend transparently
- No data loss in SQLite → Markdown migration
</success_criteria>
