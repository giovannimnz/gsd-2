---
phase: "05-db-tools-rewrite"
plan: "01"
type: implementation
wave: 1
depends_on: ["02-gsd-db-rewrite"]
files_modified:
  - src/resources/extensions/gsd/bootstrap/db-tools.ts
  - src/resources/extensions/gsd/bootstrap/query-tools.ts
autonomous: true
must_haves:
  truths:
    - All tools use StorageBackend interface
    - No direct SQLite calls remain
    - Tools work with both SQLite and Markdown backends
  artifacts:
    - db-tools.ts (updated to use StorageBackend)
    - query-tools.ts (updated to use StorageBackend)
  key_links: []
---

<objective>
Update all db-tools.ts and query-tools.ts to use the StorageBackend interface instead of direct SQLite calls.
</objective>

<tasks>
  <task>Update saveDecision tool to use StorageBackend</task>
  <task>Update updateRequirement tool to use StorageBackend</task>
  <task>Update saveRequirement tool to use StorageBackend</task>
  <task>Update writeMilestonePlanning tool to use StorageBackend</task>
  <task>Update writeSlicePlanning tool to use StorageBackend</task>
  <task>Update writeTaskPlanning tool to use StorageBackend</task>
  <task>Update completeTask tool to use StorageBackend</task>
  <task>Update completeSlice tool to use StorageBackend</task>
  <task>Update completeMilestone tool to use StorageBackend</task>
  <task>Update skipSlice tool to use StorageBackend</task>
  <task>Update saveQualityGate tool to use StorageBackend</task>
  <task>Update query tools (milestone status, requirement queries)</task>
  <task>Regenerate DECISIONS.md, REQUIREMENTS.md from StorageBackend</task>
</tasks>

<context>
db-tools.ts has ~25 tool definitions that currently call gsd-db.ts functions directly.
Each tool needs to:
1. Replace direct DB calls with StorageBackend calls
2. Maintain same prompt/description (no user-facing changes)
3. Work with both SQLite and Markdown backends

query-tools.ts has tools for reading milestone status. These need to:
1. Use StorageBackend for reads
2. Format output same as before (backward compatible)
</context>

<verification>
- [ ] No direct SQLite calls in db-tools.ts
- [ ] No direct SQLite calls in query-tools.ts
- [ ] All tools work with both backends
- [ ] DECISIONS.md regeneration works
- [ ] REQUIREMENTS.md regeneration works
- [ ] All existing tool tests pass
</verification>

<success_criteria>
- All 25+ tools use StorageBackend
- Both backends fully supported
- No regression in tool behavior
- All tests pass
</success_criteria>
