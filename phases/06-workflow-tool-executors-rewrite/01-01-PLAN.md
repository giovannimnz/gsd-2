---
phase: "06-workflow-tool-executors-rewrite"
plan: "01"
type: implementation
wave: 1
depends_on: ["05-db-tools-rewrite"]
files_modified:
  - src/resources/extensions/gsd/tools/workflow-tool-executors.ts
  - src/resources/extensions/gsd/bootstrap/dynamic-tools.ts
autonomous: true
must_haves:
  truths:
    - ensureDbOpen works with both backends
    - Tool executors gracefully degrade when backend unavailable
    - No tool fails hard when storage_backend is markdown
  artifacts:
    - workflow-tool-executors.ts (updated)
    - dynamic-tools.ts (updated ensureDbOpen)
  key_links: []
---

<objective>
Update workflow-tool-executors.ts and dynamic-tools.ts to use StorageBackend and provide graceful fallback when DB unavailable.
</objective>

<tasks>
  <task>Update ensureDbOpen() to initialize StorageBackend based on config</task>
  <task>Add isDbAvailable() → isStorageAvailable() for both backends</task>
  <task>Update all tool executors to use new storage abstraction</task>
  <task>Add graceful fallback: when storage unavailable, log warning and continue</task>
  <task>Update error messages to mention storage_backend config</task>
  <task>Add tests for storage unavailable scenarios</task>
</tasks>

<context>
workflow-tool-executors.ts has ~15 tool executors that call ensureDbOpen() and check dbAvailable.
Each needs to:
1. Use StorageBackend instead of direct DB
2. Handle storage unavailability gracefully
3. Provide informative error messages

dynamic-tools.ts has ensureDbOpen() which needs to:
1. Read storage_backend from PREFERENCES.md
2. Initialize correct backend
3. Return true if either backend is available
</context>

<verification>
- [ ] ensureDbOpen initializes correct backend based on config
- [ ] isStorageAvailable works for both backends
- [ ] All tool executors handle storage unavailability
- [ ] Error messages mention storage_backend config option
- [ ] Tests cover both backends
- [ ] Tests cover storage unavailable scenarios
</verification>

<success_criteria>
- No hard failures when storage_backend is markdown
- All tools work with both backends
- Graceful degradation when storage unavailable
- Clear error messages
</success_criteria>
