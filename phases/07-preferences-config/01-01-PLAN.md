---
phase: "07-preferences-config"
plan: "01"
type: implementation
wave: 1
depends_on: ["06-workflow-tool-executors-rewrite"]
files_modified:
  - src/resources/extensions/gsd/preferences-models.ts
  - src/resources/extensions/gsd/templates/PREFERENCES.md
  - docs/user-docs/configuration.md
autonomous: true
must_haves:
  truths:
    - storage_backend field in PREFERENCES.md
    - Migration helper: if .db exists, default to sqlite
    - Validation: only "sqlite" | "markdown" accepted
  artifacts:
    - PREFERENCES.md template (updated)
    - configuration.md docs (updated)
    - Migration helper utility
  key_links: []
---

<objective>
Add storage_backend configuration to PREFERENCES.md, create migration helper, and update all documentation.
</objective>

<tasks>
  <task>Add storage_backend field to PREFERENCES.md template</task>
  <task>Add validation for storage_backend values (sqlite | markdown)</task>
  <task>Create migration helper: sqlite-to-markdown and markdown-to-sqlite</task>
  <task>Add /gsd storage command to switch backends</task>
  <task>Update configuration.md documentation</task>
  <task>Update README.md with new config option</action>
  <task>Add integration tests for both backends</task>
  <task>Run full test suite with both backends</task>
</tasks>

<context>
PREFERENCES.md additions:
```yaml
# Storage backend for GSD state
# sqlite: uses SQLite (default, full-featured)
# markdown: uses plain .md files (portable, no DB dependency)
storage_backend: sqlite
```

Migration helper:
- `gsd storage migrate-to-markdown` — exports all data from SQLite to .md files
- `gsd storage migrate-to-sqlite` — imports all data from .md files to SQLite
- Both commands verify round-trip consistency
- Backup existing data before migration

/gsd storage command:
- Shows current backend
- Shows DB/file sizes
- Offers migration
- Validates storage health
</context>

<verification>
- [ ] PREFERENCES.md template has storage_backend field
- [ ] Validation rejects invalid values
- [ ] Migration helper works both directions
- [ ] /gsd storage command works
- [ ] Documentation updated
- [ ] Full test suite passes with both backends
</verification>

<success_criteria>
- Users can configure storage_backend in PREFERENCES.md
- Migration between backends is safe and verified
- /gsd storage command provides management UI
- Documentation is complete and accurate
- All tests pass with both backends
</success_criteria>
